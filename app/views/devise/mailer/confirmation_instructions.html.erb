<p><%= t('navigation.header.user.welcome') %> <%= @user.username.capitalize %>!</p>

<p><%= t('confirmations.confirm_account_text') %></p>

<p><%= link_to t('confirmations.confirm_account'), confirmation_url(@resource, :confirmation_token => @token) %></p>
<br/>

<%= render 'shared/mailer_footer' %>